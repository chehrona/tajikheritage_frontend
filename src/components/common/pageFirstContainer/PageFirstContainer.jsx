import React from 'react';

//Styled components
import { MainContainer } from './pageFirstContainerStyles';

export default function PageFirstContainer({ children }) {
    return <MainContainer>{children}</MainContainer>;
}
